# Database Testing Report

**Date:** 2026-07-06; **rebased 2026-07-13**  
**Platform:** Supabase (PostgreSQL)  
**Migrations reviewed:** 22 files in `supabase/migrations/` (was 19; +3 this cycle — see below)

## New migrations this cycle

| Migration | Purpose |
|-----------|---------|
| `20260711100000_bank_customers_account_sequence.sql` | Adds `bank_customers_account_number_seq` + `BEFORE INSERT` trigger to auto-generate `BOP-1000xx` account numbers; `UNIQUE(national_id)`; new INSERT RLS policy; adds table to realtime publication |
| `20260711120000_fix_bank_customers_account_sequence.sql` | Corrective: restricts the sequence fast-forward regex to the `BOP-1xxxxx` family, fixing the BOP-200013 bug class |
| `20260711130000_loan_assessment_fields.sql` | Adds 14 loan-calculator columns to `approval_requests`; extends `result_source` CHECK to 4 values; appends `NOTIFY pgrst, 'reload schema';` (added as a direct fix for BUG-014) |

## Schema entities (core)

| Table | Purpose | FK relationships | Status |
|-------|---------|------------------|--------|
| profiles | User role + metadata | auth.users | Reviewed |
| approval_requests | Credit applications | employee_id → profiles | Reviewed; **+14 new nullable columns this cycle (loan_type, loan_currency, salary_currency, monthly_obligations, client_age, loan_term_years, annual_interest_rate_used, monthly_installment, total_interest, total_repaid, debt_burden_ratio, age_at_maturity, eligibility_status, ai_explanation)** |
| loan_modification_requests | Objections | approval_request_id | Reviewed |
| documents | Document metadata | user_id, storage path | Reviewed |
| audit_logs | Immutable audit trail | user_id | Reviewed |
| bank_customers | **[Re-scoped] No longer just a demo seed — now a live INSERT target via the Open New Account flow** | — | Reviewed; **+ `UNIQUE(national_id)`, + `account_number` auto-generated by sequence/trigger, + new INSERT RLS policy, + added to realtime publication** |
| ai_chat_sessions / messages | AI history | user_id | Reviewed |
| policy_chunks | RAG embeddings | — | Reviewed |

## RLS policy verification (static)

| Table | Policy summary | Expected | Status |
|-------|----------------|----------|--------|
| approval_requests | Employee SELECT own; risk UPDATE | Per SRS | **PASS** |
| audit_logs | SELECT risk only; INSERT via trigger | Append-only | **PASS** |
| documents | SELECT all branch roles; DELETE own/all by role | Per migration | **PASS** |
| profiles | SELECT own; manager admin policies | Per migration | **PASS** |
| loan_modification_requests | Role-scoped SELECT/INSERT | Per migration | **PASS** |
| **bank_customers INSERT [new]** | Restricted to `branch_employee`/`branch_manager`; `risk` role cannot create customers | Per new migration | **PASS** static; container-verified for the insert mechanics, live role-enforcement **BLOCKED** |

## Triggers and functions

| Object | Purpose | Status |
|--------|---------|--------|
| `get_platform_stats()` | Dashboard aggregates | Static **PASS** |
| Audit log trigger on approval_requests | Auto-log status changes | Static **PASS** |
| Profile creation on signup | Auto-insert profiles row | Static **PASS** |
| `handle_updated_at` | Timestamp maintenance | Static **PASS** |
| **`trg_generate_bank_customer_account_number` [new]** | `BEFORE INSERT` trigger, fills `account_number` from `bank_customers_account_number_seq` only when NULL/blank | **PASS** — container-verified, including 20-way concurrent insert with zero collisions |

## Data integrity checks (design level)

| Check | Finding | Status |
|-------|---------|--------|
| Orphan approval_requests | employee_id FK to profiles | **PASS** design |
| Duplicate customer accounts | `UNIQUE(national_id)` constraint now enforced at the DB layer (was seed-only uniqueness) | **PASS** — container-verified, incl. the 23505 race-condition catch-and-refetch path |
| Null handling on required fields | NOT NULL on key columns; **14 new `approval_requests` columns are nullable by design (legacy rows get NULL, not an error)** | **PASS** migration |
| Uniqueness on email (profiles) | auth.users enforces | **PASS** |
| Cascade delete documents storage | Storage + DB sync in hook | Partial — live test blocked |
| **Sequence poisoning resistance [new]** | Fast-forward regex restricted to `BOP-1xxxxx`; an out-of-family row (the real `BOP-200012` anomaly found in production) cannot corrupt future numbering | **PASS** — reproduced and fixed in a Docker container |

## Seed data (PRB-027)

| Account | Purpose | loan_restricted |
|---------|---------|-----------------|
| BOP-100001 | Standard demo | false |
| BOP-100004 | Restricted customer test | true |

Static: seed migration present. Live verification **BLOCKED**. **Re-scoped note:** these seeded rows are no longer the only rows in `bank_customers` — real customer rows can now be inserted by the app; any live audit query should expect the seed range plus app-created `BOP-1000xx` rows above the seed high-water mark.

## Duplicate data risks

| Risk | Mitigation | Status |
|------|------------|--------|
| Double approval_requests for same assessment | No unique constraint on customer+timestamp | **GAP** — low demo risk, unchanged (RISK-009) |
| Duplicate document uploads | No dedup hash | **GAP** |
| Stale JWT role vs profiles.role | BUG-002 | **FAIL** |
| **Duplicate bank_customers by national_id [resolved this cycle]** | `UNIQUE(national_id)` + app-level find-first + race-safe catch-and-refetch | **PASS** — was a live bug (hard error on retry), now fixed |
| **Account number collision under concurrency [verified this cycle]** | Atomic `nextval()` on a dedicated Postgres sequence | **PASS** — 20-way concurrent insert, zero collisions, container-verified |

## Tests not executed

- Live INSERT/UPDATE/DELETE with each role
- FK violation attempts
- Concurrent update on approval_requests
- Storage orphan cleanup after failed upload
- **[New] Live confirmation of the `bank_customers` INSERT RLS policy across all 3 roles**
- **[New] Live confirmation that `NOTIFY pgrst, 'reload schema';` resolved the schema-cache issue in the actual Supabase project (BUG-014)**

## Conclusion

Database design and RLS policies **align with SRS** for graduation scope, extended this cycle to cover a genuine live write path (`bank_customers`) with concurrency-safe ID generation. Live integrity testing **BLOCKED** without Supabase credentials, same limitation as baseline; sequencing and concurrency claims are backed by Docker-container proof this cycle rather than pure static review, which is a meaningfully stronger evidence tier than the baseline had for any table.
